---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: prowlarr
spec:
  storageClassName: prowlarr
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs.${SECRET_DOMAIN}
    path: "/media/services/prowlarr"
  mountOptions:
    - nfsvers=4.2
    - nconnect=8
    - hard
    - noatime
    - nodiratime
    - nolock

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr
spec:
  storageClassName: prowlarr
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
