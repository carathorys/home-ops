---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: vaultwarden
spec:
  storageClassName: vaultwarden
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs.${SECRET_DOMAIN}
    path: "/media/services/vaultwarden"
  mountOptions:
    - nfsvers=4.2
    - nconnect=8
    - rsize=1048576
    - wsize=1048576
    - hard
    - noatime
    - nodiratime
    - nolock

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden
spec:
  storageClassName: vaultwarden
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
